/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['api.slingacademy.com', 'source.unsplash.com', 'images.unsplash.com']
    }
}

module.exports = nextConfig
